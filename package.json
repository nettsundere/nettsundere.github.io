{
  "name": "vladimir-kiselev-website",
  "version": "1.0.0",
  "description": "Personal static website with Playwright e2e tests",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "serve": "http-server -p 8080 -c-1 --silent ."
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1"
  }
}
